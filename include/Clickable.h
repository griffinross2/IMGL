#pragma once

namespace IMGL {

    // Clickable.h
    // Clickable is a base class for UI elements that can be clicked.

    class Clickable {
    public:
        Clickable(int x, int y, int width, int height);
        ~Clickable();

    };

}